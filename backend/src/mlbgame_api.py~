from flask import Flask
from mlbgame import standings

app = Flask(__name__)

@app.route('/standings/', methods=['GET'])
@app.route('/standings/<year>', methods=['GET'])
def get_standings(year=None):
    if year is not None:
        year = int(year)
    return {"result": [standing.to_json() for 
